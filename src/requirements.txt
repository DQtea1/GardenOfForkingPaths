# --- Cœur du pipeline (toujours requis) ---------------------------------
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
pyyaml>=6.0           # lecture du fichier de configuration --config

# --- Requis seulement pour certaines étapes / options --------------------
pydeseq2>=0.5         # normalisation VST par défaut (norm_method=vst) + DEGSEA (run_degsea=y)
umap-learn>=0.5.4     # embeddings UMAP ; sans lui, le pipeline tourne avec --no-umap
plotly>=5.18          # embeddings 3D interactifs (--t-SNE_dim 3)
gseapy>=1.1           # GSEA de l'étape DEGSEA (run_degsea=y)
pyarrow>=14.0         # lecture de matrices au format parquet
stabilized-ica>=2.0.0 # branche ICA stabilisée (MSTD, métagènes et projections)

# --- Note : PUREE (pureté tumorale, purity_threshold) ---------------------
# PUREE tourne en sous-processus dans son propre environnement (voir --puree_dir
# / --puree_python) : ses dépendances (scikit-learn, numpy, pandas, joblib figés)
# ne sont donc pas listées ici. Installer PUREE séparément : cf. son README.
